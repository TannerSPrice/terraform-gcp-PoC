# Configure Google Cloud Storage (GCS) Backend for Google Cloud Provider
/*terraform {
  backend "gcs" {
    bucket      = "tfstates"
    prefix      = "workload/tst"
    credentials = "service_auth_key.json"
  }
}

*/ 